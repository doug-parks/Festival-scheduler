# Supabase project credentials (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Single admin email (matches RLS policy in supabase/migrations/0002_rls.sql)
NEXT_PUBLIC_ADMIN_EMAIL=parks.doug@gmail.com

# Set once deployed (used for OAuth redirect)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
